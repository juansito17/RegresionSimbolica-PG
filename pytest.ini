[pytest]
testpaths =
    tests
markers =
    e2e: browser-based end-to-end tests
    ui: UI and callback tests
    gpu: requires a CUDA-capable GPU
    integration: cross-module integration test
addopts = -ra
cache_dir = .local/pytest
pythonpath = src
